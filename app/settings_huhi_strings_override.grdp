<?xml version='1.0' encoding='UTF-8'?>
<!-- Settings-specific Huhi strings (included from chromium_strings.grd). -->
<grit-part>
  <if expr="not chromeos">
    <message name="IDS_SETTINGS_UPGRADE_SUCCESSFUL_RELAUNCH" desc="Status label: Successfully updated Huhi">
      Nearly up to date! Relaunch Huhi to finish updating. Private windows won't reopen.
    </message>
    </if>
  <if expr="chromeos">
    </if>

  <if expr="not chromeos">
    </if>

  <if expr="chromeos">
    </if>

  <if expr="not chromeos">
  </if>
  <if expr="not chromeos">
    </if>

  <if expr="not chromeos">
    </if>

  </grit-part>
