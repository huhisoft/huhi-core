source_set("themes") {
  sources = [ "huhi_dark_mode_utils.h" ]

  deps = [
    "//base",
    "//huhi/common:pref_names",
    "//huhi/common:switches",
  ]

  if (is_android) {
    sources += [ "huhi_dark_mode_utils_android.cc" ]
  } else {
    sources += [
      "huhi_dark_mode_utils_internal.cc",
      "huhi_dark_mode_utils_internal.h",
      "theme_properties.cc",
      "theme_properties.h",
    ]

    if (is_linux) {
      sources += [ "huhi_dark_mode_utils_linux.cc" ]
    }

    if (is_mac) {
      sources += [ "huhi_dark_mode_utils_mac.mm" ]
    }

    if (is_win) {
      sources += [
        "huhi_dark_mode_utils_win.cc",
      ]
    }

    deps += [
      "//chrome/browser:theme_properties",
      "//skia:skia",
      "//ui/gfx:color_utils",
      "//ui/native_theme:native_theme",
      "//ui/native_theme:native_theme_browser",
    ]
  }
}
