import("//huhi/components/huhi_rewards/browser/buildflags/buildflags.gni")
import("//build/config/features.gni")

declare_args() {
  huhi_ads_enabled = huhi_rewards_enabled && (is_mac || is_linux || is_win || is_android)
}
