import("//huhi/components/common/typescript.gni")

transpile_web_ui("page") {
  # what is the output bundle called and what is the entry point file
  entry_points = [
    ["huhi_rewards_page", rebase_path("huhi_rewards_page.tsx")]
  ]

  # what is the directory / pack name
  resource_name = "huhi_rewards_page"
}
