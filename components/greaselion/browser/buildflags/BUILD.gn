import("//build/buildflag_header.gni")
import("//huhi/components/greaselion/browser/buildflags/buildflags.gni")

buildflag_header("buildflags") {
  header = "buildflags.h"
  flags = [
    "ENABLE_GREASELION=$enable_greaselion",
  ]
}
